# Session Notes — 2026-05-31

## What shipped today

### 1. Guard — Settings page crashes fixed (prod)
- `TypeError` on `notify_on_block` / `notify_on_budget` / `alert_channel` — React updater closures captured `undefined` from async call
- Fixed `handleToggle` to use local `value` directly, not `data[field]`
- Fixed `handleSaveChannel` to use `channelInput || null`

### 2. Guard — Slack notification bug fixed
- `_send_guard_slack` was looking up Integration by `team.conductai_org_id` (string) instead of `team.workspace_id` (UUID FK) — never matched
- Fixed in `apps/api/app/modules/guard/routers/events.py`

### 3. Guard — Spend controls backend
- `GET /guard/spend` now accepts `month` param
- `GET /guard/spend/budget-check` — no Clerk auth, checks hard cap for hook use
- Spend page fixed: resolves team via `/guard/teams/me`, correct field mappings (`tokens_after` not `tokens_used`, etc.)

### 4. Guard — Hard cap enforcement in hook
- PreToolUse hook now checks `/guard/spend/budget-check` on every call (5-min cache at `~/.guard/budget_cache.json`)
- If `hard_blocked: true` → `sys.exit(2)`, call blocked before hitting model

### 5. Guard — DSL block complete
- Added `"guard"` to `SUPPORTED_BLOCK_TYPES` in `apps/api/app/dsl/schema.py`
- Rewrote `_execute_guard` in executor: team lookup, context serialization, policy evaluation, audit event write, enforcement_mode dispatch
- **New today**: `guard_check` run event emitted inline in the workflow trace so Guard decisions appear alongside block events in the run feed

### 6. Guard — MCP server (`conductguard-mcp`)
- New binary: `packages/conduct-cli/src/conduct_cli/guardmcp.py`
- JSON-RPC 2.0 over stdio, protocol version `2024-11-05`
- Three tools: `guard_status`, `guard_check`, `guard_sync`
- Registered in `pyproject.toml` as `conductguard-mcp` entry point

### 7. Guard — Dual-auth for hook/MCP
- `get_guard_hook_auth` dependency in `app/core/auth.py` — accepts Clerk JWT OR GuardMember member_token
- `GET /guard/policies/sync` switched to this auth so `conduct guard sync` and MCP tools work

### 8. Marketing — Guard landing page copy
- Updated trust strip (hard cap enforcement bullet)
- Step 3 onboarding card: removed "Coming soon", shows `conductguard-mcp` live
- New "Spend Controls" section: hard cap mechanics, Slack alerts
- New "Observability — Three audit layers" section: run trace / workspace audit / Guard audit cards with `guard_check` bridge callout

### 9. Marketing — FAQ updated
- 6 new Guard-specific FAQ entries covering: what Guard is, local vs workflow enforcement, spend cap mechanics, enforcement_mode when Guard not installed, per-rule targeting, Guard vs approval gates

### 10. Docs page updated
- Sidebar: Guard block + ConductGuard nav sections added
- New sections: Guard block YAML reference, conduct guard join, PreToolUse hook flow, conductguard-mcp MCP tools, Spend controls API

### 11. README rewritten
- Hero + What it does updated with Guard + Memory in the flow
- ConductGuard section: team lead setup, `conduct guard join`, hook, MCP, YAML block
- Agent Memory section with YAML example and scope comparison
- Observability section: three audit layers table
- Block types table now includes Memory and Guard

### 12. Guard — 33 unit tests
- `apps/api/tests/test_guard.py` — fully hermetic (no DB, no network)
- Covers: enforcement_mode, match_tool/pattern/path, context_keys, guard-not-installed, output shape, model field validation for all 5 Guard models
- Real SQLAlchemy model introspection via stubbed SQLite Base

### 13. Guard — Dashboard seed script
- `apps/api/scripts/seed_guard.py`
- Creates: 5 developers, 5 policies, team + per-dev budgets, 30 days of sessions + ~200–400 audit events
- `--clean` flag wipes and reloads; `--dry-run` wipes only

---

## Pending / Pick up tomorrow

### A. PyPI trusted publishing (conduct-cli)
**Status:** Blocked on one-time pypi.org config — no code work needed.

**Action for Sudhi:**
1. Go to pypi.org → sign in → conduct-cli project → Settings → "Add a new publisher"
2. Fill in:
   - Owner: `sseshachala`
   - Repository: `conductai`
   - Workflow filename: `publish-cli.yml`
   - Environment: `pypi`
3. Save — then re-run the GitHub Actions `publish-cli.yml` workflow
4. Verify `pip install conduct-cli` installs v0.3.1

### B. Agent Booster — enforce Read/Grep redirect at CLI level
**Design agreed:** Booster installs a PreToolUse hook (same mechanism as Guard hook) that intercepts native `Read` and `Grep` calls and exits with a redirect message.

**Questions to resolve before building:**
- Hard block (exit 2) or soft warn (exit 1)? Leaning toward: files >50 lines → block, small files → pass through
- The hook needs a size threshold check (can stat the file from the hook payload)
- `booster install-hook` command to install it (mirrors `conduct guard join`)
- Should auto-install when user runs `pip install agent-booster`

**Files to touch:**
- New: `~/.claude/hooks/booster_read_guard.py` — the hook script
- `packages/conduct-cli/` or a separate `agent-booster` package — the install command

### C. Screenshots for README / landing page
**What's needed:**
- Canvas with a Guard block visible mid-graph
- Guard spend dashboard (developer budget bars)
- Run trace showing `guard_check` event inline
- Guard audit log table (decision/tool/rule columns)

**Where to add:** `docs/screenshots/` folder → reference in `README.md` with `![caption](docs/screenshots/name.png)`

### D. Role-aware Guard UI (parked — come back later)
**Context:** Admin sees full Guard config, editor sees policies + audit only, viewer sees own spend + audit read-only. Backend `role` field on `GuardMember` is ready. Frontend shows everything to everyone today.
**When to pick up:** When team governance becomes a blocker — not before.

---

## How to run tests

```bash
# Guard unit tests (33, fully hermetic)
/path/to/marshal/apps/api/.venv/bin/python -m pytest apps/api/tests/test_guard.py -v

# Seed Guard dashboard with test data
python apps/api/scripts/seed_guard.py --workspace-id <uuid>

# Wipe + reload
python apps/api/scripts/seed_guard.py --workspace-id <uuid> --clean
```

## Key commits today
- `04e5197` Guard marketing copy + docs
- `84db2a8` Guard FAQ questions
- `20b3f2c` guard_check run event + observability landing section
- `a90dd5f` README rewrite
- `2be0f17` 33 Guard tests + seed script
