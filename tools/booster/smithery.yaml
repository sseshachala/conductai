name: agent-booster
version: 0.1.2
description: AST+vector context router for AI coding agents — cut token costs 5-15x by sending only relevant symbols instead of full files.

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    () => ({
      command: "booster",
      args: ["serve"],
      env: {}
    })
