[
  {
    "type": "function",
    "function": {
      "name": "get_spend_summary",
      "description": "Get spend KPIs, cost breakdown by AI tool and developer",
      "parameters": {
        "type": "object",
        "properties": {
          "month": {"type": "string", "description": "YYYY-MM filter, omit for all-time"}
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_recent_events",
      "description": "Get Guard audit events for trend and aggregation analysis",
      "parameters": {
        "type": "object",
        "properties": {
          "limit":    {"type": "integer", "description": "Number of events, default 20, max 100"},
          "decision": {"type": "string",  "description": "Filter: block, warn, or audit"},
          "since":    {"type": "string",  "description": "ISO-8601 start timestamp"},
          "until":    {"type": "string",  "description": "ISO-8601 end timestamp"},
          "rule_id":  {"type": "string",  "description": "Filter by rule"}
        },
        "required": []
      }
    }
  }
]
