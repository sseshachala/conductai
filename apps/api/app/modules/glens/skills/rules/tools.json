[
  {
    "type": "function",
    "function": {
      "name": "list_policies",
      "description": "List all Guard policies for the workspace (custom rules + enabled state)",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_policy",
      "description": "Get full details of a single policy by rule_id",
      "parameters": {
        "type": "object",
        "properties": {
          "rule_id": {"type": "string", "description": "The rule_id of the policy to fetch"}
        },
        "required": ["rule_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_knowledge",
      "description": "Semantic search across Guard knowledge index — finds rules, audit events, and discovered agents by intent or description",
      "parameters": {
        "type": "object",
        "properties": {
          "q": {"type": "string", "description": "Natural language search query"},
          "kind": {"type": "string", "enum": ["rule", "audit_event", "discovered_agent"], "description": "Filter by source kind (omit to search all)"},
          "limit": {"type": "integer", "default": 10}
        },
        "required": ["q"]
      }
    }
  }
]
