[
  {
    "type": "function",
    "function": {
      "name": "search_sessions",
      "description": "Semantic search over session reports using pgvector",
      "parameters": {
        "type": "object",
        "properties": {
          "q":     {"type": "string",  "description": "Natural language search query"},
          "limit": {"type": "integer", "description": "Number of results, default 5, max 20"}
        },
        "required": ["q"]
      }
    }
  }
]
